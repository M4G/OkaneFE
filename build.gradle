allprojects {
    apply plugin: 'java'
    apply plugin: 'idea'
    apply from: "${rootProject.projectDir}/dependencies.gradle"
}


sourceCompatibility = 1.5

version = '1.0'

subprojects {
    repositories {
        mavenLocal()
        mavenCentral()
    }
}